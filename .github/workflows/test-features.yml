name: Test Package Features

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test-features:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install PyYAML

      - name: Test snippet syntax
        run: |
          if [ -f "Snippets/NunjucksToolbox.sublime-snippets" ]; then
            echo "✅ Snippets file found"
            python3 -c "import json; json.load(open('Snippets/NunjucksToolbox.sublime-snippets')); print('✅ Snippets JSON is valid')"
          else
            echo "⚠️ No snippets file found"
          fi

      - name: Test completions syntax
        run: |
          if [ -f "Completions/NunjucksToolbox.sublime-completions" ]; then
            echo "✅ Completions file found"
            python3 -c "import json; json.load(open('Completions/NunjucksToolbox.sublime-completions')); print('✅ Completions JSON is valid')"
          else
            echo "⚠️ No completions file found"
          fi

      - name: Test build system
        run: |
          if [ -f "BuildSystems/NunjucksToolboxBuildSystems.sublime-build" ]; then
            echo "✅ Build system found"
            python3 -c "import json, sys; build = json.load(open('BuildSystems/NunjucksToolboxBuildSystems.sublime-build')); required = ['shell_cmd', 'selector']; missing = [k for k in required if k not in build]; print('❌ Missing keys:', missing) if missing else print('✅ Build system valid'); sys.exit(1) if missing else None"
          else
            echo "⚠️ No build system file found"
          fi

      - name: Test preference files
        run: |
          found_prefs=false
          for file in Preferences/*.tmPreferences; do
            if [ -f "$file" ]; then
              found_prefs=true
              echo "✅ Found preference file: $file"
              python3 -c "import xml.etree.ElementTree as ET; ET.parse('$file'); print('✅ $file is valid XML')" || echo "❌ XML error in $file"
            fi
          done
          if [ "$found_prefs" = false ]; then
            echo "⚠️ No preference files found"
          fi

      - name: Test syntax file structure
        run: |
          python3 << 'EOF'
          import yaml, sys
          try:
              with open('Syntaxes/NunjucksToolbox.sublime-syntax') as f:
                  syntax = yaml.safe_load(f)
              required = ['name', 'file_extensions', 'scope', 'contexts']
              missing = [k for k in required if k not in syntax]
              if missing:
                  print('❌ Missing syntax keys:', missing)
                  sys.exit(1)
              contexts = syntax.get('contexts', {})
              if 'main' not in contexts:
                  print('❌ Missing main context')
                  sys.exit(1)
              print('✅ Syntax file structure valid')
              print('  - Name:', syntax.get('name'))
              print('  - Extensions:', syntax.get('file_extensions'))
              print('  - Contexts:', len(contexts))
          except Exception as e:
              print('❌ Error validating syntax file:', e)
              sys.exit(1)
          EOF

      - name: Test file completeness
        run: |
          echo "📋 Package completeness check:"

          # Check required files
          missing_required=0

          if [ -f "Syntaxes/NunjucksToolbox.sublime-syntax" ]; then
            echo "✅ Required: Syntaxes/NunjucksToolbox.sublime-syntax"
          else
            echo "❌ Missing required: Syntaxes/NunjucksToolbox.sublime-syntax"
            missing_required=$((missing_required + 1))
          fi

          if [ -f "README.md" ]; then
            echo "✅ Required: README.md"
          else
            echo "❌ Missing required: README.md"
            missing_required=$((missing_required + 1))
          fi

          # Check optional files
          optional_count=0

          if [ -f "Snippets/NunjucksToolbox.sublime-snippets" ]; then
            echo "✅ Optional: Snippets/NunjucksToolbox.sublime-snippets"
            optional_count=$((optional_count + 1))
          fi

          if [ -f "Completions/NunjucksToolbox.sublime-completions" ]; then
            echo "✅ Optional: Completions/NunjucksToolbox.sublime-completions"
            optional_count=$((optional_count + 1))
          fi

          if [ -f "BuildSystems/NunjucksToolboxBuildSystems.sublime-build" ]; then
            echo "✅ Optional: BuildSystems/NunjucksToolboxBuildSystems.sublime-build"
            optional_count=$((optional_count + 1))
          fi

          if [ -f "Settings/NunjucksToolboxAutoPairs.sublime-settings" ]; then
            echo "✅ Optional: Settings/NunjucksToolboxAutoPairs.sublime-settings"
            optional_count=$((optional_count + 1))
          fi

          pref_files=$(ls -1 Preferences/*.tmPreferences 2>/dev/null | wc -l)
          if [ "$pref_files" -gt 0 ]; then
            echo "✅ Optional: $pref_files preference files"
            optional_count=$((optional_count + 1))
          fi

          if [ $missing_required -gt 0 ]; then
            echo "❌ Missing $missing_required required files"
            exit 1
          fi

          echo "📊 Summary:"
          echo "  - Required files: 2/2 ✅"
          echo "  - Optional features: $optional_count found"

      - name: Test feature integration
        run: |
          echo "🔧 Testing feature integration:"

          # Test if all scopes match
          main_scope="text.html.nunjucks-toolbox"

          # Check syntax file scope
          syntax_scope=$(python3 -c "import yaml; print(yaml.safe_load(open('Syntaxes/NunjucksToolbox.sublime-syntax')).get('scope', ''))")
          if [ "$syntax_scope" = "$main_scope" ]; then
            echo "✅ Syntax scope matches: $syntax_scope"
          else
            echo "❌ Syntax scope mismatch: $syntax_scope vs $main_scope"
          fi

          # Check other files scope consistency
          for file in Preferences/*.tmPreferences; do
            if [ -f "$file" ]; then
              scope=$(python3 -c "import xml.etree.ElementTree as ET; root=ET.parse('$file').getroot(); elements = [elem.text for elem in root.iter() if elem.text and 'nunjucks-toolbox' in elem.text]; print(elements[0] if elements else 'none')")
              if [ "$scope" = "$main_scope" ]; then
                echo "✅ $file scope matches"
              else
                echo "⚠️ $file scope: $scope"
              fi
            fi
          done

      - name: Validate package structure
        run: |
          echo "🔍 Validating package structure:"

          # Test that all files use consistent scopes
          python3 << 'EOF'
          import yaml
          import xml.etree.ElementTree as ET
          import json
          import os

          main_scope = "text.html.nunjucks-toolbox"
          issues = []

          # Check syntax file
          try:
              with open('Syntaxes/NunjucksToolbox.sublime-syntax') as f:
                  syntax = yaml.safe_load(f)
              if syntax.get('scope') != main_scope:
                  issues.append(f"Syntax file scope mismatch: {syntax.get('scope')}")
              else:
                  print("✅ Syntax file scope correct")
          except Exception as e:
              issues.append(f"Error reading syntax file: {e}")

          # Check snippets/completions files
          for filename in ['Snippets/NunjucksToolbox.sublime-snippets', 'Completions/NunjucksToolbox.sublime-completions']:
              if os.path.exists(filename):
                  try:
                      with open(filename) as f:
                          data = json.load(f)
                      if data.get('scope') != main_scope:
                          issues.append(f"{filename} scope mismatch: {data.get('scope')}")
                      else:
                          print(f"✅ {filename} scope correct")
                  except Exception as e:
                      issues.append(f"Error reading {filename}: {e}")

          # Check build system
          if os.path.exists('BuildSystems/NunjucksToolboxBuildSystems.sublime-build'):
              try:
                  with open('BuildSystems/NunjucksToolboxBuildSystems.sublime-build') as f:
                      build = json.load(f)
                  if build.get('selector') != main_scope:
                      issues.append(f"Build system selector mismatch: {build.get('selector')}")
                  else:
                      print("✅ Build system selector correct")
              except Exception as e:
                  issues.append(f"Error reading build system: {e}")

          # Check preference files
          for filename in os.listdir('Preferences'):
              if filename.endswith('.tmPreferences'):
                  try:
                      tree = ET.parse(f'Preferences/{filename}')
                      root = tree.getroot()
                      scopes = [elem.text for elem in root.iter() if elem.text and 'nunjucks-toolbox' in elem.text]
                      if scopes and main_scope in scopes:
                          print(f"✅ Preferences/{filename} scope correct")
                      else:
                          issues.append(f"Preferences/{filename} scope issue: {scopes}")
                  except Exception as e:
                      issues.append(f"Error reading Preferences/{filename}: {e}")

          if issues:
              print("❌ Package structure issues found:")
              for issue in issues:
                  print(f"  - {issue}")
              exit(1)
          else:
              print("✅ Package structure validation passed")
          EOF

      - name: Generate feature report
        run: |
          echo "# Package Feature Report" > feature-report.md
          echo "" >> feature-report.md
          echo "## Available Features" >> feature-report.md

          if [ -f "Syntaxes/NunjucksToolbox.sublime-syntax" ]; then
            echo "- ✅ **Syntax Highlighting** (Syntaxes/NunjucksToolbox.sublime-syntax)" >> feature-report.md
          fi

          if [ -f "Snippets/NunjucksToolbox.sublime-snippets" ]; then
            echo "- ✅ **Code Snippets** (Snippets/NunjucksToolbox.sublime-snippets)" >> feature-report.md
          fi

          if [ -f "Completions/NunjucksToolbox.sublime-completions" ]; then
            echo "- ✅ **Auto-completion** (Completions/NunjucksToolbox.sublime-completions)" >> feature-report.md
          fi

          if [ -f "BuildSystems/NunjucksToolboxBuildSystems.sublime-build" ]; then
            echo "- ✅ **Build System** (BuildSystems/NunjucksToolboxBuildSystems.sublime-build)" >> feature-report.md
          fi

          if [ -f "Settings/NunjucksToolboxAutoPairs.sublime-settings" ]; then
            echo "- ✅ **Auto-Pairing** (Settings/NunjucksToolboxAutoPairs.sublime-settings)" >> feature-report.md
          fi

          pref_count=$(ls -1 Preferences/*.tmPreferences 2>/dev/null | wc -l)
          if [ "$pref_count" -gt 0 ]; then
            echo "- ✅ **Editor Preferences** ($pref_count .tmPreferences files)" >> feature-report.md
            for file in Preferences/*.tmPreferences; do
              if [ -f "$file" ]; then
                echo "  - $file" >> feature-report.md
              fi
            done
          fi

          echo "" >> feature-report.md
          echo "## Test Results" >> feature-report.md
          echo "- ✅ All syntax files validated" >> feature-report.md
          echo "- ✅ JSON/XML structure verified" >> feature-report.md
          echo "- ✅ Scope consistency checked" >> feature-report.md
          echo "- ✅ File completeness verified" >> feature-report.md
          echo "- ✅ Package structure validated" >> feature-report.md

          echo "" >> feature-report.md
          echo "## File Extensions Supported" >> feature-report.md
          echo "- .nunjucks" >> feature-report.md
          echo "- .nunjs" >> feature-report.md
          echo "- .njk" >> feature-report.md
          echo "- .html (with Nunjucks syntax)" >> feature-report.md

          cat feature-report.md

      - name: Upload feature report
        uses: actions/upload-artifact@v4
        with:
          name: feature-report
          path: feature-report.md
